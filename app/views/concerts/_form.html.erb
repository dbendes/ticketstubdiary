<%= form_for(@concert) do |f| %>
  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.hidden_field :user_ids %> 
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :venue_tokens, "Venue" %><br>
    <%= f.text_field :venue_tokens %>
  </div>
    <div class="field">
    <%= f.label :band_tokens, "Bands" %><br>
    <%= f.text_field :band_tokens %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
